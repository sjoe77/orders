<div class="d-flex justify-content-between align-items-center mt-3">
  <div class="text-muted">
    <%= pagination_info(customers) %>
  </div>
  <nav aria-label="Table pagination">
    <%= paginate_collection(customers, current_params) %>
  </nav>
</div>